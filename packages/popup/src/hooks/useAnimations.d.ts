/**
 * Hook for animating number counts from 0 to target value
 */
export declare function useCountUp(target: number, duration?: number): number;
/**
 * Hook to trigger fade-in animation on mount
 */
export declare function useFadeIn(delay?: number): boolean;
